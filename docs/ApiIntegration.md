# API integration

This guide has been consolidated into [Networking](Networking.md), which reflects the current Dio, Retrofit, interceptor, environment, and `Api.call` implementation.

Keep this file as a compatibility link for existing bookmarks. Use `Networking.md` for new documentation and changes.
